

// if(input.length>0) {
//     const num_parts: string[] = inputString.split(",")
//     if(num_parts.length===1) return parseInt(num_parts[0])
//     return parseInt(num_parts[0]) + parseInt(num_parts[1])
// }
